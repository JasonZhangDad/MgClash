# MgClash 1.1.0

> **Unsigned release:** MgClash has no Apple Developer ID, no notarization, no
> Microsoft Authenticode signature, no signed Linux repository, and no GPG
> signature. A checksum is the only integrity check on offer — verify
> `SHA256SUMS`, or the `.sha256` beside your download, before running anything.

## What changed since 1.0.3

The interface was rebuilt. The shell is no longer a menu bar over a split pane;
it is a fixed sidebar with one page per task.

- **There is now a Connect button.** Through 1.0.3 the only way to start a
  session was to double-click a node row — nothing on screen said "connect".
  Connect / Disconnect now sits in the top bar and on the Overview hero.
- **Navigation.** A 212px sidebar with Overview, Nodes, Proxies, Connections,
  Traffic, Logs, a Configuration group holding Routing and DNS, and Settings at
  the foot. The three window layouts of 1.0.3 are gone: they decided where
  content sat without ever answering which page you were on.
- **Routing, DNS, and Settings are pages, not dialogs.** All three have tables,
  remote state, and a restart-the-Core side effect that a dialog could not
  carry. Dialogs are now only confirmations, imports, and QR codes; node and
  connection detail open in drawers; mode switches open in popovers.
- **Overview.** A connection hero with the active node, its protocol, latency,
  transport, and Core, over a routing-mode switch, System Proxy mode, and the
  TUN toggle — with the reason spelled out when TUN is unavailable.
- **Nodes.** Subscriptions moved in as a third tab beside All and Groups, since
  the only thing a subscription produces is nodes. The table keeps column
  resizing, adds a card view, and the node inspector opens as a drawer with
  per-protocol fields rather than one universal form.
- **Command palette** on ⌘K / Ctrl+K: connect, switch node, test latencies,
  update subscriptions, change routing mode, jump to logs, or search nodes.
- **Traffic history.** 1.0.3 kept only live rates and running totals. A daily
  aggregate now persists, so the Traffic page draws a real per-day series.
- **Light and dark** were re-derived from one semantic token set: green means
  connected, teal means active, amber means warning, red means error.

Every capability of 1.0.3 is still present; nothing was removed to make room.

Every download here is built from one commit.

A cross-platform desktop proxy client that drives external Cores. It implements
no proxy protocol itself: sing-box and Xray do the work, and MgClash decides
which one can run a node, generates its configuration, and manages its
lifecycle.

## Downloads

**macOS Intel**
- Installer: `mgclash-1.1.0-macos-x86_64-unsigned.dmg`
- Archive: `mgclash-1.1.0-macos-x86_64-unsigned.tar.gz`

**macOS Apple Silicon**
- Installer: `mgclash-1.1.0-macos-aarch64-unsigned.dmg`
- Archive: `mgclash-1.1.0-macos-aarch64-unsigned.tar.gz`

**Windows x86_64**
- Installer: `mgclash-1.1.0-windows-x86_64-unsigned-setup.exe`
- Portable executable: `mgclash-1.1.0-windows-x86_64-unsigned-portable.exe`
- Portable archive: `mgclash-1.1.0-windows-x86_64-unsigned.zip`

**Linux x86_64**
- Debian/Ubuntu: `mgclash-1.1.0-linux-x86_64-unsigned.deb`
- Fedora/RHEL: `mgclash-1.1.0-linux-x86_64-unsigned.rpm`
- Portable: `mgclash-1.1.0-linux-x86_64-unsigned.AppImage`
- Archive: `mgclash-1.1.0-linux-x86_64-unsigned.tar.gz`

The `.dmg`, `.deb`, `.rpm`, and `.AppImage` are conveniences, not provenance:
none of them is signed, and the OS will warn exactly as it does for the
archives.

Every download has an individual `.sha256` sidecar. `SHA256SUMS` covers the
complete release set.

## What it does

- **Nodes**: VLESS, VMess, Trojan, Shadowsocks, Hysteria2, TUIC, SOCKS5, HTTP,
  WireGuard, AnyTLS, Naive, and raw custom Core configs. Import by share link,
  by file, by QR image, or with the camera; export the same way.
- **Subscriptions**: scheduled updates, include/exclude filters, a custom
  User-Agent, and subconverter support.
- **Routing**: rule / global / direct modes, named schemes, and rules matching
  domain, suffix, keyword, IPv4 and IPv6 CIDR, port, network, sniffed protocol,
  inbound, process name and path, GeoIP/GeoSite, and remote rule sets.
- **DNS**: system, UDP, TCP, DoH, DoT, address strategies, hosts, and FakeIP.
- **Local proxying**: SOCKS and HTTP inbounds, optional LAN exposure, System
  Proxy in managed / PAC / cleared / unchanged modes, with the host's previous
  proxy state snapshotted and restored.
- **TUN** on all three platforms, behind the desktop's own authorization prompt.
- **Live status**: Clash-API traffic and connections, latency and download speed
  tests, policy groups with automatic re-measurement, a log panel, and a
  redacted diagnostic bundle.
- Ten interface languages, global hotkeys, and a tray.

## Platform warnings

- **macOS** Gatekeeper will warn on first launch; open the app explicitly
  through Finder or System Settings. TUN asks for administrator authorization
  when you enable it — macOS opens a `utun` device for root only. Stopping a TUN
  session asks a second time, because the app cannot signal a process it does
  not own.
- **Windows** SmartScreen will warn for both the portable executable and the
  installer. Select **More info → Run anyway** only after verifying the
  checksum. TUN asks for UAC elevation.
- **Linux** ships as a tarball rather than through a signed package repository.
  TUN asks through polkit and needs a running authentication agent.

## Bundled

The official **sing-box 1.13.18** Core is included and pinned by SHA-256;
Windows also includes the official Authenticode-verified **Wintun 0.14.1**
driver library.

**Xray is not bundled.** The release build downloads and verifies Xray 26.3.27
but never copies it into the artifact, so choosing the Xray Core fails with
`xray_unavailable` until you download it from inside the app (Settings → Core).
Earlier release notes claimed Xray shipped in the bundle; that was wrong, and it
was wrong for 1.0.x too. Nothing about sing-box changes.

Downloading a newer Core from within the app verifies it against the digest its
publisher ships, or — for sing-box, which publishes none — records the first
download's digest and refuses a later one that disagrees.

## Known limits

- Nothing here is signed. On every platform the OS will treat it as software
  from an unidentified developer. On macOS that means a double-click is refused
  until you either right-click the app and choose **Open**, or clear the
  quarantine flag with `xattr -dr com.apple.quarantine /Applications/MgClash.app`.
- **The default ports collide with most other proxy clients.** MgClash listens
  on SOCKS 10808 and HTTP 10809, which is exactly what v2rayN and its relatives
  use. If one of those is already running, connecting fails with
  `local Socks proxy port 127.0.0.1:10808 is unavailable: Address already in use`.
  Change the ports in Settings → Network, or stop the other client first.
- The authorization prompts, camera scanning, and real TUN traffic are verified
  by hand rather than by the test suite; automated coverage stops at the
  boundary where a human has to approve something.
- A saved config override is used verbatim and does not follow a later change of
  node or route. The window says so while one is in force.
- The new interface is verified by unit and component tests, a type check, and a
  release build. It has not been through a hands-on pass on Windows or Linux —
  layout at the 1100×700 lower bound in particular is untested on those two.
- Traffic history starts accumulating at first launch of 1.1.0. Upgrading does
  not backfill days spent on an earlier version, because no per-day totals were
  kept before this release.
